# FlashEvolve — Paper Project Site

Auto-generated by `lab.picasso-lab.com` on 2026-05-12 from the paper PDF +
Overleaf source zip.

## Files
- `index.html` — the page itself
- `style.css` — dark-mode product-page styles (tritongym lineage)
- `app.js` — minimal interactivity (filter chips, etc.)
- `figures/Figure_N.png` — extracted from the published `paper.pdf` via the
  lab's `paper_figures.py` (figures are taken from compiled paper, NOT the
  Overleaf source `pic/` directory, which is often a stale draft)

## To preview locally
```bash
python3 -m http.server 8000
# then open http://localhost:8000
```

## To edit
This is a plain static site — open `index.html` in any editor.

## Live URL
https://flashevolve.picasso-lab.com

## Notes
- The "Read Paper (PDF)" button links to `https://arxiv.org/pdf/2605.08520`.
  **Never host the local NeurIPS submission PDF on a public domain** — it
  carries a "Do not distribute" watermark and would trigger desk rejection.
